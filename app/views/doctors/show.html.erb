<h2> Hello <%= @doctor.name %>! </h2>

<h3>Appointments: </h3><br />
<ul>
    <% @doctor.appointments.each do |appointment| %>
        <li> <%= appointment.datetime %> with <%= link_to appointment.patient.name, patient_path(appointment.patient)%> </li>   
    <% end %>
</ul>